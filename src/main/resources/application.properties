# POC API pur (JAX-RS/Jersey) + Swagger UI servi en statique.
# Déployé en WAR sur un Tomcat externe (docker-compose -> ROOT.war, context "/").
server.servlet.context-path=/

# Utilisateur fixe pour tester Spring Security au curl (POC uniquement).
spring.security.user.name=admin
spring.security.user.password=admin
